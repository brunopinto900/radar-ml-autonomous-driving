# Core
numpy==2.5.1
h5py==3.16.0
matplotlib==3.11.1
pandas==3.0.5
pyarrow==25.0.0

# Notebooks
jupyterlab==4.6.2
ipykernel==7.3.0

# RadarScenes dataset loader + Qt-based point cloud/label viewer (rad_viewer)
# Pulls in PySide6, pyqtgraph, QDarkStyle, scipy, Pillow as dependencies.
radar_scenes==1.0.4

# Deep learning (GPU) - install the CUDA build matching your driver from
# https://pytorch.org/get-started/locally/ instead of a plain `pip install torch`
# if this line fails to resolve.
torch==2.11.0+cu128